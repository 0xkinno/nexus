[package]
name = "nexus"
version = "0.1.0"
edition = "2021"

[dependencies]
sails-rs = "0.10.4"

[build-dependencies]
gear-wasm-builder = "=1.10.0"

[lib]
name = "nexus"
crate-type = ["cdylib", "rlib"]

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
